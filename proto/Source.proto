syntax = "proto3";

package org.vocalsharp.vocalshaper.vsp4;

message Source {
	enum Type {
		AUDIO = 0;
		MIDI = 1;
		SYNTH = 2;
	}

	Type type = 1;
	string path = 2;
	string name = 3;
}

message SourceList {
	repeated Source sources = 1;
}
